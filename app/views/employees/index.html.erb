<div class="w-full">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Employees</h1>
    <%= link_to "New employee", new_admin_employee_path(@admin), class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>

  <div id="employees" class="min-w-full">
    <%= render @employees %>
  </div>
</div>
